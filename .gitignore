# -----------------------------------
# ARCHIVO .gitignore CONSOLIDADO
# -----------------------------------

# 1. ARTEFACTOS DE COMPILACIÓN DE JAVA Y MAVEN
# Ignora todos los directorios 'target/' en cualquier nivel del proyecto (incluye el Fat JAR final)
**/target/
# Archivos de clase compilados (redundante con **/target/ pero buena práctica)
*.class
# Archivos de paquetes (JAR, WAR, etc. - redundante con **/target/)
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# 2. ARCHIVOS DE LOG Y CRASH DE MÁQUINA VIRTUAL
*.log
hs_err_pid*
replay_pid*

# 3. ARCHIVOS Y CACHÉS DE IDEs
# IntelliJ
.idea/
*.iml
# VS Code
.vscode/

# 4. ARCHIVOS DE SISTEMA OPERATIVO
.DS_Store
Thumbs.db

# 5. ESPECÍFICO DEL PROYECTO (ourcrud-java)
# Directorio de salida del LOG SQL del módulo DB
sql_output/
# Archivo de contexto generado por FileProcessor para la IA
contexto_completo.txt
# Archivos de código corregido generados por AIAnalyzer
*-corregido.java